/**
 * Shared configuration constants.
 *
 * Leaf module with no imports from sibling config files — exists to break the
 * circular dependency between RepoConfigDefaults and RepoConfigSchema.
 */

export const DEFAULT_GITHUB_API_VERSION = '2022-11-28';
